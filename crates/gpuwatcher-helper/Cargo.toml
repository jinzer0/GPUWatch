[package]
name = "gpuwatcher-helper"
version = "0.1.0"
edition = "2021"

[dependencies]
gpuwatcher-core = { path = "../gpuwatcher-core" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
tokio = { version = "1.48.0", features = ["rt", "time"] }
